[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"

[project]
name = "plaidnox-sast-agent"
version = "0.1.0"
description = "Context-aware, static-only source code security analysis pipeline"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.115,<1.0",
  "Jinja2>=3.1,<4.0",
  "jsonschema>=4.23,<5.0",
  "PyYAML>=6.0",
  "SQLAlchemy>=2.0,<3.0",
  "psycopg[binary]>=3.2,<4.0",
  "tree-sitter>=0.25,<0.27",
  "tree-sitter-language-pack>=1.9,<3.0",
  "uvicorn[standard]>=0.34,<1.0",
]

[project.optional-dependencies]
ai = ["litellm>=1.101,<2.0", "perplexityai>=0.43,<1.0"]
observability = ["opentelemetry-api>=1.35,<2.0", "opentelemetry-sdk>=1.35,<2.0"]

[project.scripts]
plaidnox-sast = "plaidnox_sast.cli:main"
plaidnox-scm = "plaidnox_scm.cli:main"
plaidnox-scm-api = "plaidnox_scm.api_main:main"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
plaidnox_sast = ["assets/**/*.json", "assets/**/*.md", "assets/**/*.sql"]
plaidnox_scm = ["assets/**/*.json", "assets/**/*.md", "assets/**/*.sql"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
