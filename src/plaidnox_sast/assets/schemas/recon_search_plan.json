{
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "strategy": {"type": "string", "minLength": 1, "maxLength": 5000},
    "queries": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "query_id": {"type": "string", "minLength": 1, "maxLength": 120},
          "pattern": {"type": "string", "minLength": 1, "maxLength": 2000},
          "include_globs": {
            "type": "array",
            "items": {"type": "string", "minLength": 1, "maxLength": 300}
          },
          "objective": {"type": "string", "minLength": 1, "maxLength": 2000},
          "coverage_targets": {
            "type": "array",
            "minItems": 1,
            "items": {"type": "string", "minLength": 1, "maxLength": 500}
          }
        },
        "required": ["query_id", "pattern", "include_globs", "objective", "coverage_targets"]
      }
    },
    "coverage_notes": {"type": "string", "minLength": 1, "maxLength": 5000}
  },
  "required": ["strategy", "queries", "coverage_notes"]
}
