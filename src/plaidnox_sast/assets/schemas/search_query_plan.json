{
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "strategy": {"type": "string", "minLength": 1, "maxLength": 4000},
    "queries": {
      "type": "array",
      "minItems": 1,
      "maxItems": 300,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "query_id": {"type": "string", "minLength": 1, "maxLength": 120},
          "task_ids": {
            "type": "array",
            "minItems": 1,
            "maxItems": 100,
            "items": {"type": "string", "minLength": 1, "maxLength": 120}
          },
          "pattern": {"type": "string", "minLength": 1, "maxLength": 2000},
          "include_globs": {
            "type": "array",
            "maxItems": 100,
            "items": {"type": "string", "minLength": 1, "maxLength": 300}
          },
          "objective": {"type": "string", "minLength": 1, "maxLength": 2000}
        },
        "required": ["query_id", "task_ids", "pattern", "include_globs", "objective"]
      }
    },
    "coverage_notes": {"type": "string", "minLength": 1, "maxLength": 4000}
  },
  "required": ["strategy", "queries", "coverage_notes"]
}
